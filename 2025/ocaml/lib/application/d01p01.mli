val solve : string -> unit -> int
(** [solve input ()] is the solution for the first day of advent of code 2025 for [input].
    The solution for an empty input is [Domain.Day_1.Dial.(to_int start)].
    Requires: [input] to be a multiline string using `\n` as it's line separator. *)
